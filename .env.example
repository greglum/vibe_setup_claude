# SEMOSS Credentials (required for MCP servers)
# Set these as environment variables before starting Claude:
#   export SEMOSS_ACCESS_KEY="your-access-key"
#   export SEMOSS_SECRET_KEY="your-secret-key"

# Project Configuration
PROJECT_ID=your-project-uuid-here

# SEMOSS Instance (defaults used if not set)
# SEMOSS_URL=https://workshop.cfg.deloitte.com/
# SEMOSS_DB_ID=your-database-engine-id
